package it.polimi.ingsw.server.controller.enums;

/**
 * this enum is used to distinguish if the performPlace method is called to place the first or the second worker for the currentPlayer
 */
public enum PlacePart {
    FIRST,
    SECOND
}
